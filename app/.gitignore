# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Headless-review harness (fake admin/user + fixtures, Playwright screenshot
# script) - committed deliberately now (was previously gitignored wholesale,
# which meant every container recycle destroyed it). Only its throwaway
# output is ignored.
review/shots/
review/node_modules/
